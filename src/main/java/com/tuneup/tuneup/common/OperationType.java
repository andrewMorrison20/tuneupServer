package com.tuneup.tuneup.common;


/**
 * Represents the type of operation being performed (e.g., CREATE, UPDATE).
 * Used across multiple validators and services.
 */
public enum OperationType {
    CREATE,
    UPDATE,
    DELETE
}
