# Security enable/disable
# src/test/resources/application-test.yml

# ✅ Existing security override
security:
  basic:
    enabled: false


spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
